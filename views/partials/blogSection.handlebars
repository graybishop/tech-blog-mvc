<div class="col-md-6 mb-4 p-2">
    <div class="border rounded shadow p-2">
        <div class="d-flex border-bottom justify-content-between align-items-baseline mb-2">
            <h2><a href="/post/{{id}}">{{name}}</a></h2>
            <p class="fs-5 mb-0">Authored by {{user.username}} on {{format_date createdAt}}</p>
        </div>
        <div>
            <p>{{description}}</p>
        </div>

    </div>
</div>