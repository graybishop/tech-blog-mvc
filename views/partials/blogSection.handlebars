<div class="row">
    <div class="d-flex border-bottom justify-content-between align-items-end">
        <h2>{{name}}</h2>
        <h4>Authored by {{user.username}} on {{format_date createdAt}}</h4>
    </div>
    <div>
        <p>{{description}}</p>
    </div>
</div>