<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body class="container">
    <nav class="row fs-4 bg-dark py-4">
      <div class="col d-flex justify-content-center">
        <div class="btn btn-primary">
          <a href="/" class="text-light text-decoration-none fw-bold">Home</a>
        </div>
      </div>
      <div class="col d-flex justify-content-center">
        <div class="btn btn-primary">
          <a href="/dashboard" class="text-light text-decoration-none fw-bold">Dashboard</a>
        </div>
      </div>
      {{#if loggedIn}}
      <div class="col d-flex justify-content-center">
        <div class="btn btn-warning">
          <a href="" class="text-dark text-decoration-none fw-bold" id="logout">Logout</a>
        </div>
      </div>
      {{else}}
      <div class="col d-flex justify-content-center">
        <div class="btn btn-primary">
          <a  href="/login" class="text-light text-decoration-none fw-bold">Login</a>
        </div>
      </div>
      {{/if}}
    </nav>
    <main class="row">
    {{{body}}}
    </main>
      {{! Render script for logged in users only}}
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
  </body>
</html>
